# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/

# Rasa
# Removed: models/*.tar.gz - We need model.tar.gz for Railway deployment
.rasa/
# Exclure SEULEMENT le modèle sentiment (XLM-RoBERTa), pas les modèles Rasa entraînés
# models/ est inclus volontairement pour les fichiers Rasa

# MongoDB
*.log

# Modèles lourds (hébergés sur Hugging Face)
# On garde SEULEMENT les fichiers nécessaires dans le repo (ex: metadata.json, model.tar.gz)
# et on ignore tous les artefacts du modèle RoBERTa local.
models/model.pt
models/*.h5
models/*.pkl
models/config.json
models/tokenizer.json
models/tokenizer_config.json
models/sentencepiece.bpe.model
models/special_tokens_map.json
models/Train.py
models/*.png
models/overfiting.md
models/resumeEpoch.txt

# Credentials et secrets
.env
# credentials.yml (Uncommented for deployment)
# credentials.yml
credentials_prod.yml

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Streamlit
.streamlit/secrets.toml

# Backup
backup/
*.backup
