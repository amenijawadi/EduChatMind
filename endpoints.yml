# Action server configuration
action_endpoint:
  url: "http://localhost:5055/webhook"

# Tracker store for persistence
tracker_store:
  type: mongod
  url: ${MONGODB_URI}
  db: "rasa"
  collection: "tracker"

#tracker_store:
  #type: sql
  #dialect: "sqlite"
  #db: "conversations.db"
  
# Event broker for logging
#event_broker:
#  type: "file"
#  path: "logs/events.log"