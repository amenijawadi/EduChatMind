# Action server configuration
action_endpoint:
  url: "http://localhost:5055/webhook"

# CORS configuration for cross-origin requests (Streamlit Cloud)
cors:
  origins: ["*"]
  methods: ["GET", "POST", "PUT", "DELETE", "OPTIONS"]
  allow_credentials: true
  expose_headers: ["*"]

# Tracker store for persistence (Disabled temporarily to fix crashes)
#tracker_store:
#  type: mongod
#  url: ${MONGODB_URI}
#  db: "rasa"
#  collection: "tracker"

#tracker_store:
  #type: sql
  #dialect: "sqlite"
  #db: "conversations.db"
  
# Event broker for logging
#event_broker:
#  type: "file"
#  path: "logs/events.log"